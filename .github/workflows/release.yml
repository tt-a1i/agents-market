name: Release

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Version tag to publish, for example v0.1.0"
        required: true

permissions:
  contents: write
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          registry-url: https://registry.npmjs.org
      - run: npm ci
      - name: Validate dispatch version
        if: github.event_name == 'workflow_dispatch'
        run: |
          expected="v$(node -p "require('./package.json').version")"
          if [ "${{ inputs.version }}" != "$expected" ]; then
            echo "Workflow input version must match package.json: expected $expected, got ${{ inputs.version }}"
            exit 1
          fi
      - run: npm run release:check
      - run: npm run release:artifacts -- --out ./release-artifacts --catalog-base-url "https://tt-a1i.github.io/agents-market" --release-tag "${GITHUB_REF_NAME}"
      - uses: actions/upload-artifact@v4
        with:
          name: agents-market-release-artifacts
          path: ./release-artifacts
      - run: npm publish --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
      - name: Attach artifacts to GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release view "${GITHUB_REF_NAME}" >/dev/null 2>&1 || gh release create "${GITHUB_REF_NAME}" --title "${GITHUB_REF_NAME}" --notes "Agents Market ${GITHUB_REF_NAME}"
          gh release upload "${GITHUB_REF_NAME}" release-artifacts/*.tgz release-artifacts/install.sh release-artifacts/SHA256SUMS release-artifacts/release-artifacts.json release-artifacts/registry.bundle.json --clobber
